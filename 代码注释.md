# 像素蛋糕（Pixel Cake）代码注释 / 架构设计文档

> 本文档详细描述本项目（Tauri 2 + Rust + React 的本地 AI 修图客户端）的
> **代码架构、分层职责、参数传递链路、模块功能注释**，供开发者快速上手与维护。
>
> 适用版本：v0.2.1（M6 打磨基本完成，v0.2.1 已发布；含 AI 模型输入输出约定实机验证结论）

---

## 目录

1. [项目总览](#1-项目总览)
2. [分层架构总览](#2-分层架构总览)
3. [核心协议层 crates/bus](#3-核心协议层-cratesbus)
4. [数据层 crates/storage](#4-数据层-cratesstorage)
5. [引擎层 crates/engine](#5-引擎层-cratesengine)
6. [调度层 crates/scheduler](#6-调度层-cratesscheduler)
7. [Tauri 应用层 apps/desktop](#7-tauri-应用层-appsdesktop)
8. [前端 React 层](#8-前端-react-层)
9. [参数传递链路全解析](#9-参数传递链路全解析)
10. [SQLite 数据表结构](#10-sqlite-数据表结构)
11. [目录结构与代码规模](#11-目录结构与代码规模)
12. [关键设计决策与约定](#12-关键设计决策与约定)
13. [已知风险与后续工作](#13-已知风险与后续工作)

---

## 1. 项目总览

**像素蛋糕** 是一款 Windows 11 桌面本地 AI 修图客户端，复刻「像素蛋糕」核心能力：
中性灰磨皮、AI 美型（液化）、祛瑕、AI 追色、基础调色（曝光/白平衡/曲线/颗粒/暗角）、
滤镜、批量导出。所有数据本地存储，AI 推理全部在本地 NVIDIA GPU（RTX 3070，8GB）完成。

### 1.1 技术栈

| 层 | 技术 |
|---|---|
| 外壳 | Tauri 2（Rust 后端 + WebView2 前端） |
| 前端 | React 18 + TypeScript + Vite 5 |
| 图像引擎 | Rust，16bit f32 全链路（线性/ProPhoto 色彩空间） |
| AI 推理 | ONNX Runtime（CUDA EP），`ort` crate（2.0.0-rc.13，cuda feature） |
| 存储 | SQLite（`rusqlite` bundled，静态编译） |
| 调度 | Tokio 队列 + GpuBudget（8GB 显存预算） |
| 打包 | NSIS 安装包（GitHub Actions windows-latest CI 产出） |

### 1.2 交付硬约束

- 用户只需安装 **VC++ 运行库 + 标准 NVIDIA 驱动**，无需 CUDA Toolkit / cuDNN / Python / .NET。
- 外壳锁定 Tauri（规避 WinUI3 的 .NET 依赖）；运行期零 Python。
- CUDA 运行时与 cuDNN 随安装包内置；ONNX Runtime 二进制随 crate 构建下载。
- AI 模型文件不入库：用户通过「设置 → 模型下载」从 hf-mirror.com 下载到 `app_data/models/`。

---

## 2. 分层架构总览

```
┌────────────────────────────────────────────────────────────────────┐
│  ① React 前端（apps/desktop/src）                                   │
│     App.tsx（状态编排） · 面板（磨皮/美型/追色/调色/祛瑕/滤镜/预设）   │
│     Canvas（WebGL2 实时预览 + PNG 回退） · api/client.ts（IPC 封装） │
└───────────────┬────────────────────────────────────────────────────┘
                │ invoke() / listen()        （Tauri IPC）
┌───────────────▼────────────────────────────────────────────────────┐
│  ② Tauri 命令层（apps/desktop/src-tauri）                           │
│     main.rs（注册 18 个命令） · commands.rs（IPC 薄封装）            │
│     state.rs（AppState） · events.rs（事件转发） · models.rs（下载） │
└──────┬──────────────────────┬───────────────────────────┬──────────┘
       │ Store（数据）          │ Scheduler（调度）           │ 事件流
┌──────▼──────┐        ┌───────▼────────────────────┐     │
│ ④ storage   │        │ ② scheduler（Tokio 队列 +   │─────┘
│    SQLite   │        │     GpuBudget 8GB + 广播）  │ engine://event
│    门面     │        └───────┬────────────────────┘
└─────────────┘                │ EngineRequest
                     ┌─────────▼──────────────────────────┐
                     │ ③ engine（核心重算力，16bit 全链路） │
                     │  image 缓冲 / 色彩空间 / 算子抽象     │
                     │  base 调色 · color(LUT/柔光/频率分离) │
                     │  retouch 磨皮/美型/祛瑕/追色/增强     │
                     │  pipeline 全链路 · raw 解码           │
                     │  infer ORT 封装 · detect SCRFD/2DFAN4/│
                     │  BiSeNet · ai 门面 RetouchEngine     │
                     └────────────────────────────────────┘
```

**数据流总览**（一次「调整滑块 → 看到预览」的完整往返）：

```
前端滑块 ─→ updateRecipe(recipe)
  ├─ 纯参数（WebGL 模式）：save_recipe 入库 + ensureBase 取底图 → GPU shader 即时渲染（零延迟）
  └─ 含 AI 操作（后端模式）：save_recipe 入库 + 200ms 防抖 → submit_render("preview")
       ─→ Scheduler 队列 → process_request：解码→降采样→AI→管线→PNG
       ─→ EngineEvent::Done → read_preview → base64 → <img> 显示
```

---

## 3. 核心协议层 crates/bus

**文件**：`crates/bus/src/lib.rs`（303 行）

**定位**：全程编辑协议 —— UI ⇄ 引擎 ⇄ 存储三方共享的唯一数据契约。
所有类型实现 `Serialize + Deserialize`，可直接 `serde_json` 往返。

### 3.1 `Recipe` —— 编辑协议（六个子模块）

`Recipe` 是 UI 与引擎之间唯一的编辑契约，JSON 序列化后落 SQLite。

```rust
pub struct Recipe {
    pub neutral_gray: NeutralGray,   // 中性灰磨皮（柔光合成双蒙版）
    pub beauty: Beauty,              // AI 美型 / 液化
    pub inpaint: Vec<InpaintRegion>, // 祛瑕（多边形区域列表）
    pub color: Color,                // AI 追色 / 调色
    pub base: Base,                  // 基础调色（曝光/白平衡/曲线…）
    pub filter: Option<Filter>,      // 滤镜（LUT ID + 强度）
}
```

#### 3.1.1 `NeutralGray` —— 中性灰磨皮

```rust
pub struct NeutralGray {
    pub enabled: bool,
    pub ka: u8,              // 平整强度 0–100（去斑、平滑）
    pub kb: u8,              // 立体强度 0–100（保留光影结构）
    pub mode: NeutralGrayMode, // Dual / FlatOnly / StructureOnly
}
```

`mode` 语义（`NeutralGrayMode` 枚举）：
- `Dual`：平整 + 立体双蒙版合成（先平整后立体，见 §5.6 softlight 公式）。
- `FlatOnly`：仅平整（纯磨皮）。
- `StructureOnly`：仅立体（只保留光影结构）。

#### 3.1.2 `Beauty` —— AI 美型 / 液化

```rust
pub struct Beauty {
    pub enabled: bool,
    pub face_slim: f32,        // 瘦脸 0–100
    pub body_slim: f32,        // 瘦身 0–100
    pub neck_slim: f32,        // 天鹅颈 0–100
    pub face_full: f32,        // 面部丰盈 0–100
    pub manual_points: Vec<ControlPoint>, // 用户拖拽控制点（归一化坐标）
}
```

`ControlPoint`（用户手动微调）：

```rust
pub struct ControlPoint {
    pub x: f32,   // 归一化 x（0..1）
    pub y: f32,   // 归一化 y（0..1）
    pub dx: f32,  // x 方向位移（归一化）
    pub dy: f32,  // y 方向位移（归一化）
}
```

> 注意：`Beauty` 中的 `body_slim` / `neck_slim` / `face_full` 目前是 UI 参数，
> 引擎液化当前只消费 `manual_points` 与 AI 关键点生成的瘦脸/大眼点（见 §5.10 `face_beauty_points`）。

#### 3.1.3 `InpaintRegion` —— 祛瑕

```rust
pub struct InpaintRegion {
    pub polygon: Vec<Point>, // 归一化多边形顶点（用户画笔采集）
    pub kind: InpaintKind,   // 瑕疵类型
}
pub enum InpaintKind { Blemish, Tattoo, Background, Teeth }
```

#### 3.1.4 `Color` —— AI 追色

```rust
pub struct Color {
    pub enabled: bool,
    pub lut_ref: Option<String>,          // 已烘焙 3D LUT 引用（路径/ID）
    pub reference_path: Option<String>,   // 参考样片路径（现场烘焙用）
    pub per_region_strength: f32,         // 分区独立追色强度 0–1
    pub mode: ColorTransferMode,          // Extreme（极致，强度1.0）/ Harmony（和谐，0.6）
}
```

#### 3.1.5 `Base` —— 基础调色

```rust
pub struct Base {
    pub exposure: f32,      // 曝光 EV，-5..5
    pub contrast: f32,      // 对比度 -100..100
    pub temperature: f32,   // 色温 -100..100（正=暖）
    pub tint: f32,          // 色调 -100..100
    pub curves: Vec<Point>, // 曲线锚点（归一化 0..1，空=直通）
    pub hsl: Hsl,           // 色相/饱和度/明度（引擎只消费 saturation）
    pub grain: f32,         // 颗粒 0–100
    pub vignette: f32,      // 暗角 0–100
}
```

#### 3.1.6 `Filter` —— 滤镜

```rust
pub struct Filter {
    pub lut_id: String, // 滤镜 LUT ID（内置 warm/cool/bw/vivid，.cube 可扩展）
    pub intensity: f32, // 强度 0–1（向恒等 LUT 混合）
}
```

### 3.2 `Scope` —— 重算范围（三模式）

```rust
pub enum Scope {
    Base,    // 仅解码 + 降采样，不跑任何调色管线（WebGL 实时预览的底图）
    Preview, // 代理图预览（完整管线，最长边可配置，默认 1600px）
    Export,  // 全分辨率导出
}
```

这是「编辑即所见」性能策略的关键：**Base 底图 + 前端 GPU 实时调色** 让纯参数
编辑零延迟；**Preview** 供含 AI 的操作生成完整 PNG 预览；**Export** 全分辨率出图。

### 3.3 `EngineRequest` —— UI → 引擎的提交载荷

```rust
pub struct EngineRequest {
    pub photo_id: String,          // 照片 ID（事件回流识别）
    pub raw_path: String,          // 原始文件路径（引擎据此解码输入）
    pub recipe: Recipe,            // 完整编辑协议
    pub scope: Scope,              // Base / Preview / Export
    #[serde(default)] pub export_dir: Option<String>,      // 导出目录（None=与源同目录）
    #[serde(default)] pub preview_max_edge: Option<u32>,   // 预览最长边（None=默认1600）
    #[serde(default)] pub export_format: Option<String>,   // "tiff"|"png"（None=默认tiff）
}
```

### 3.4 `EngineEvent` —— 引擎 → UI 的事件

以 `type` 字段判别（serde tag），前端监听 `engine://event`：

```rust
#[serde(tag = "type", rename_all = "snake_case")]
pub enum EngineEvent {
    Progress { photo_id, step: PipelineStep, pct: f32 }, // 进度（0..100）
    Done { photo_id, result_path: Option<String>, proxy_updated: bool },
    Error { photo_id, code: ErrorCode, message: String },
}
```

`PipelineStep`（进度步骤）：`RawDecode → NeutralGray → BeautyWarp → Inpaint → ColorLut → BaseTone → Filter → Encode`。

`ErrorCode`：`Io / Decode / Inference / OutOfMemory / CudaUnavailable / InvalidRecipe`。

### 3.5 模型清单

`ModelSpec { id, name, url, size_bytes, purpose }` + `ModelStatus`（`not_downloaded / downloading / downloaded`），
用于「设置 → 模型下载」面板。

---

## 4. 数据层 crates/storage

**文件**：`crates/storage/src/lib.rs`（383 行）

**定位**：SQLite 数据访问门面。元数据入库，大文件（原图/代理/成品/模型/LUT）只存路径。

### 4.1 `Store` 结构

```rust
pub struct Store {
    conn: Mutex<Connection>, // rusqlite::Connection 非 Sync，用 Mutex 包裹满足 Tauri State 要求
}
```

### 4.2 核心方法签名

| 方法 | 签名 | 说明 |
|---|---|---|
| `open` | `fn open(path) -> Result<Self>` | 打开（不存在则创建）数据库 + 初始化 schema |
| `create_project` | `fn create_project(&self, name, root_path) -> Result<Project>` | 新建项目（客片组） |
| `list_projects` | `-> Result<Vec<Project>>` | 按创建时间倒序 |
| `import_photos` | `fn import_photos(&self, project_id, paths: &[String], dims: impl Fn(&str)->(u32,u32)) -> Result<Vec<Photo>>` | **`dims` 回调注入尺寸探测**（由命令层传 `engine::raw::probe_dimensions`，解耦存储与引擎） |
| `list_photos` / `get_photo` | 按项目 / 按 ID | |
| `update_photo_status` | `(photo_id, status)` | pending / retouched / exported |
| `save_recipe` | `(photo_id, &Recipe)` | `ON CONFLICT(photo_id) DO UPDATE` upsert |
| `get_recipe` | `(photo_id) -> Result<Option<Recipe>>` | JSON 反序列化回 `Recipe` |
| `save_preset` / `list_presets` | 预设（保存的编辑参数） | |
| `register_model` | `(m: &ModelMeta)` | 模型注册（upsert） |
| `get_setting` / `set_setting` | key/value upsert | 用户设置 |

### 4.3 关键设计

- **`recipe` 表以 `photo_id` 为主键**，一张照片一份 recipe，upsert 幂等。
- **`dims` 回调解耦**：`import_photos` 不依赖 `engine` crate，尺寸探测由调用方注入，
  保持 storage 独立可测（测试用 `|_| (4000, 6000)` 桩）。
- **schema 常量** `SCHEMA`：6 张表（见 §10）。
- 模型文件**不入库**：`register_model` 只存元数据（id/name/path/version/hash/enabled）。

---

## 5. 引擎层 crates/engine

**目录**：`crates/engine/src/`，9 个模块（见 `lib.rs`）：

```
engine/src/
  lib.rs        模块声明（ai/base/color/detect/export/image/infer/pipeline/raw/retouch）
  image.rs      ImageBuf（16bit f32 四平面）+ ColorSpace + ImageOp + OpCost + sRGB/Lab 转换
  pipeline.rs   16bit 全链路（固定算子顺序）
  base/tone.rs  基础调色（曝光/对比度/白平衡/饱和度/曲线/颗粒/暗角）
  color/        softlight.rs（柔光/中性灰合成）· freq.rs（频率分离）· lut.rs（3D LUT）
  retouch/      neutral_gray.rs（磨皮）· beauty.rs（液化）· inpaint.rs（祛瑕）
                color_transfer.rs（Lab 追色）· enhance.rs（GPEN 皮肤增强）
  raw.rs        RAW 解码（PPM / LibRaw / JPEG/PNG）
  infer.rs      ONNX Runtime 推理封装（CUDA EP）
  detect/       face.rs（SCRFD）· landmark.rs（2DFAN4）· segment.rs（BiSeNet）
  ai/mod.rs     RetouchEngine 门面（惰性加载 4 模型 + 美型点生成）
  export.rs     16bit TIFF / 8bit PNG 编码
```

### 5.1 `image.rs` —— 16bit 图像缓冲与算子抽象

**核心结构**：

```rust
pub struct ImageBuf {
    pub width: u32,
    pub height: u32,
    pub space: ColorSpace,
    planes: [Vec<f32>; 4],   // RGBA 四平面，每像素 f32
}
```

**`ColorSpace`**：

```rust
pub enum ColorSpace {
    SRgb,      // sRGB（gamma 编码，仅编解码边界）
    Linear,    // 线性 RGB（D65）—— 管线内部主空间
    ProPhoto,  // ProPhoto/ROMM RGB（D50，宽色域，导出保真）—— 占位
    Lab,       // CIELAB（追色 μ/σ 迁移）
}
```

**安全设计**：

```rust
pub const MAX_PIXELS: usize = 200_000_000; // 约 2 亿像素（远超 8K）
```

`ImageBuf::new` 用 `checked_mul` 检测溢出并校验 `<= MAX_PIXELS`，防止恶意/损坏的文件头
导致超大内存分配（OOM 防护，见 §12.4 安全加固）。

**关键方法**：
- `pixel(x, y) -> [f32; 4]` / `set_pixel(x, y, c)`：行优先索引读写。
- `plane(ch) -> &[f32]` / `plane_mut(ch)`：暴露单平面供算子/着色器/FFI 直接消费。
- `from_srgb_rgba8`：8bit sRGB → 线性（RAW 之外的导入边界）。

**色彩空间转换**（均为 `#[inline]` 单像素函数）：
- `srgb_to_linear` / `linear_to_srgb`：IEC 61966-2-1（0.04045 / 0.0031308 分段）。
- `rgb_to_lab` / `lab_to_rgb`：线性 sRGB(D65) → XYZ(D65) → CIELAB（D65 参考白），追色用。

**算子抽象**：

```rust
pub trait ImageOp: Send + Sync {
    fn apply(&self, src: &ImageBuf, dst: &mut ImageBuf);
    fn cost(&self) -> OpCost;
}

pub struct OpCost {
    pub vram_bytes: usize,  // 估算峰值显存（分块推理 tile 大小）
    pub weight: f32,        // 相对耗时权重（1=轻量，>1=重算）
    pub exclusive_gpu: bool,// 是否独占 GPU（AI 推理=true）
}
```

> 目前 `OpCost` 已实现于各算子，scheduler 仍以固定估算占位（见 §6），
> 后续可将 `OpCost` 接入 `GpuBudget` 做动态显存调度。

### 5.2 `pipeline.rs` —— 16bit 全链路

**固定算子顺序**（与 Photoshop 精修直觉一致）：

```rust
pub fn process(src: &ImageBuf, p: &Pipeline) -> ImageBuf {
    // 1. 中性灰磨皮（柔光合成 NeutralGrayCompose）
    // 2. 美型液化（liquify）
    // 3. 祛瑕（inpaint，4 次迭代扩散）
    // 4. 追色 LUT（lut.apply_image）
    // 5. 基础调色（ToneAdjust）
    // 6. 滤镜 LUT（lut.apply_image）
}
```

```rust
#[derive(Default)]
pub struct Pipeline {
    pub neutral_gray: Option<NeutralGrayInput>, // 蒙版已由 GAN 预测 + 上采样到原图尺寸
    pub beauty_points: Vec<LiquifyPoint>,       // 液化控制点
    pub inpaint_mask: Option<ImageBuf>,         // 祛瑕 mask（瑕疵=1）
    pub color_lut: Option<Lut3D>,               // 追色 LUT
    pub tone: ToneParams,                       // 基础调色
    pub filter_lut: Option<Lut3D>,              // 滤镜 LUT
}
```

> **关键设计**：AI 预测（人脸检测、蒙版、分割）全部在上层（scheduler + RetouchEngine）完成，
> 其结果（蒙版/控制点/LUT）作为本管线输入 —— 管线保持**纯图像处理**，可独立测试（如
> `empty_pipeline_is_identity` 测试）。

### 5.3 `base/tone.rs` —— 基础调色

**单像素数学 `tone_pixel`**（`#[inline]`，线性 RGB 0..1）：

```rust
// 顺序：白平衡 → 曝光 → 对比度 → 饱和度
// 白平衡：温度影响 R/B，色调影响 G
r *= 1.0 + temp * 0.5;   b *= 1.0 - temp * 0.5;   g *= 1.0 + tint * 0.3;
// 曝光：2^EV
r *= 2^exposure; ...
// 对比度（中点 0.5）
c = (c - 0.5) * (1 + contrast/100) + 0.5;
// 饱和度（Rec.709 亮度）
gray = 0.2126r + 0.7152g + 0.0722b;
c = gray + (c - gray) * (1 + saturation/100);
```

**`ToneAdjust` 算子**（`ImageOp`）在 `tone_pixel` 基础上叠加：
- **曲线**：`curve_lookup` 锚点间分段线性插值（空曲线=直通）。
- **暗角**：`vignette_factor`，中心 1，角落 `1 - strength/100 * d²`。
- **颗粒**：`hash_noise` 确定性伪随机噪声（像素坐标哈希），三通道同源。

> **前端 WebGL shader 精确复刻此数学**（见 §8.3），保证「WebGL 实时预览」与
> 「后端全链路出图」结果一致。

### 5.4 `color/lut.rs` —— 3D LUT

**核心结构**：

```rust
pub struct Lut3D {
    pub size: u32,          // 33³ 或 17³
    table: Vec<[f32; 3]>,   // 表序 b 最快：index = (r*n + g)*n + b
}
```

**构建方式**：
- `identity(size)`：恒等 LUT（输出=输入）。
- `from_fn(size, f)`：从映射函数构建，对每个格点调用 `f(输入RGB) -> 输出RGB`（追色烘焙用）。
- `from_cube(text)`：解析 Adobe `.cube` 文本（`.cube` 条目 red 最快，映射到内部 b 最快表序）。

**查询**：`apply(rgb)` 三线性插值（8 个邻近格点，x/y/z 方向插值）。

**强度混合**：

```rust
pub fn blended(&self, intensity: f32) -> Self {
    // intensity=0 → 恒等；1 → 全量。out = c + (lut(c) - c) * t
    Self::from_fn(self.size, |c| { let o = self.apply(c); [c0 + (o0-c0)*t, ...] })
}
```

> **修复记录**：滤镜强度 UI 不生效问题的根因是导出未做强度混合，`blended` 修复后
> 前后端强度一致。

**内置滤镜**（`builtin_filter_lut`，无 `.cube` 时的兜底预设）：

| ID | 变换 |
|---|---|
| `warm` | R×1.08, G×1.02, B×0.9（暖） |
| `cool` | R×0.9, G×1.0, B×1.1（冷） |
| `bw` | BT.601 亮度 l = 0.299R+0.587G+0.114B（黑白） |
| `vivid` | 三通道 ×1.15（鲜艳） |

### 5.5 `color/softlight.rs` —— 柔光与中性灰合成

**W3C Soft Light 混合**（输入输出 0..1 线性）：

```rust
pub fn softlight(base, blend) -> f32 {
    if blend <= 0.5 { base - (1 - 2*blend) * base * (1 - base) }
    else {
        let d = if base <= 0.25 { ((16*base-12)*base+4)*base } else { base.sqrt() };
        base + (2*blend - 1) * (d - base)
    }
}
```

**中性灰磨皮单像素合成**（双参数）：

```text
tmp = Softlight(S, A) * ka + S * (1 - ka)      // 平整层
D   = Softlight(tmp, B) * kb + tmp * (1 - kb)  // 立体层
```

> 语义：A=平整蒙版（GAN 预测的无斑驳平滑层），B=立体蒙版（光影结构层）。
> `Softlight(base, 0.5) == base` 恒等 —— 蒙版值为 0.5 表示「不改变」。

**算子**：`NeutralGrayCompose { mask_flat, mask_struct, ka, kb }`，`ka/kb` 从 0..100 归一化到 0..1。

### 5.6 `color/freq.rs` —— 频率分离（磨皮基础）

- `gaussian_blur(src, sigma)`：**separable** 高斯模糊（水平 pass → 垂直 pass），核半径 `3σ`。
- `split_frequency`：`low = blur(src)`，`high = src - low`（高频可含负值，16bit float 支持）。
- `recombine`：`low + high` 重组。

> 去瑕保纹理的原理：磨皮只处理低频层，再重组回高频。目前被 GPEN 磨皮路径替代，
> 频率分离保留为独立可测的算子。

### 5.7 `retouch/beauty.rs` —— 美型液化

```rust
pub struct LiquifyPoint {
    pub x: f32,       // 归一化坐标 0..1
    pub y: f32,
    pub dx: f32,      // 位移（归一化）
    pub dy: f32,
    pub radius: f32,  // 影响半径（归一化）
}
```

**`liquify(src, points)` 算法**（反向采样）：
1. 对每个输出像素，遍历所有控制点，累积推拉位移：
   - 距离 `d2 < r²` 时，权重 `weight = (1 - t²)²`（`t = d/r`，平滑衰减）。
   - `mx += p.dx * weight`。
2. 从原图**反向采样** `sample_bilinear(src, (px-mx)*w, (py-my)*h)`。

> 反向采样保证输出图像无空洞。权重函数使控制点中心影响最大、边缘平滑过渡。

### 5.8 `retouch/inpaint.rs` —— 祛瑕

- `polygon_to_mask(w, h, polygon)`：归一化多边形 → 栅格化 mask（**射线法**点内判定）。
- `merge_mask(src, add)`：多区域 mask 逻辑或。
- `inpaint(src, mask, iterations)`：mask 区域用**邻域非 mask 像素均值**填充，迭代扩散（默认 4 次）。

> 这是 AI inpaint 网络接入前的兜底实现；接口 `inpaint(src, mask)` 保持不变，
> 生产可替换为 ONNX 推理（MIGAN/轻量扩散）。

### 5.9 `retouch/color_transfer.rs` —— Lab 追色

**Reinhard 色彩迁移**（CIELAB 空间均值-方差迁移）：

```rust
// 单像素迁移
fn transfer_pixel(rgb, src: &LabStats, dst: &LabStats, mode) -> [f32; 3] {
    let strength = match mode { Extreme => 1.0, Harmony => 0.6 };
    let lab = rgb_to_lab(rgb);
    for i in 0..3 {
        let ratio = if src.std[i] < 1e-6 { 1.0 } else { dst.std[i] / src.std[i] };
        let target = (lab[i] - src.mean[i]) * ratio + dst.mean[i];
        out[i] = lab[i] + (target - lab[i]) * strength;  // 强度混合
    }
}
```

**烘焙为 LUT**（性能关键）：
- `build_transfer_lut(target, reference, mode, size)`：全局迁移 → 33³ LUT。
- `build_region_transfer_lut(target, target_mask, reference, ref_mask, mode, size)`：
  分区（皮肤/发/唇…）独立统计迁移，**只迁指定区域**（`masked_stats` 只统计 mask>0.5 像素）。
- `per_region_transfer`：多分区逐区迁移（`RegionMask` 结构）。

> **为什么烘焙 LUT**：每个像素做 Lab 转换 + 统计迁移很昂贵；离散化为 33³ 表 +
> 三线性插值后，预览与批量只需查表，性能提升数个量级。

### 5.10 `retouch/enhance.rs` —— GPEN 皮肤增强

**链路**：bbox 裁剪 → resize 512² → RGB + `x*2-1`（[-1,1]）→ 前向输出增强人脸 →
`(x+1)/2` 回 [0,1] sRGB → `srgb_to_linear` → resize 回 crop 尺寸 → **blend 融合**回原图 bbox 区域。

```rust
pub fn enhance(&mut self, img, bbox, blend) -> Result<ImageBuf, String> {
    // blend = 增强结果权重 0..1（磨皮强度 ka/100）
    // out = o * (1-blend) + e * blend   （仅 bbox 区域）
}
```

> GPEN 是专有中性灰 GAN 的开源等价物，用作「磨皮」的皮肤增强替代。
> 模型输入输出约定为 GPEN 标准（`x*2-1` 归一化）。

### 5.11 `raw.rs` —— RAW / 图像解码

**解码器抽象**：

```rust
pub trait RawDecoder: Send + Sync {
    fn decode(&self, bytes: &[u8]) -> Result<ImageBuf, DecodeError>;
}
```

**`decode_auto(path, bytes)` 按扩展名分发**：

| 扩展名 | 解码器 | 说明 |
|---|---|---|
| `ppm` / `pnm` | `PpmDecoder` | 纯 Rust，8bit/16bit P6 |
| `cr2 cr3 arw nef dng raf orf rw2 pef srw` | `LibRawDecoder` | `libraw-rs` FFI，输出 16bit sRGB → 转线性 |
| `jpg jpeg png` | `decode_image` | `image` crate，sRGB → 线性 |

**安全加固**：
- PPM 头部宽高用 `checked_mul` + `MAX_PIXELS` 校验，防恶意头 OOM（返回 `Err` 而非 panic）。
- 数据长度不足返回 `Truncated`。

**`probe_dimensions(path)`**：只读文件头快取尺寸（不全量解码），导入时批量探测。

### 5.12 `infer.rs` —— ONNX Runtime 推理封装

**`InferSession`**：持有 ORT `Session`，注册 CUDA 执行提供者（`ep::CUDA`）。

```rust
pub fn run(&mut self, input_name: &str, tensor: Tensor<f32>, output_names: &[&str])
    -> Result<Vec<(Vec<usize>, Vec<f32>)>, String>;  // 返回 (shape, 行优先展平 f32)
```

**预处理**：`image_to_nchw(img, w, h)` —— 双线性 resize → 线性转 sRGB → clamp [0,1] → NCHW `[1,3,H,W]`。

**`resize_bilinear`**：保持 RGBA 四通道的双线性缩放（全引擎共用）。

> 运行时要求：CUDA ≥ 13.2 + cuDNN ≥ 9.23（见 ort 文档）。注册失败时 ort 默认
> 静默回退 CPU —— 这是降级策略的一环（模型缺失或 CUDA 不可用时不崩溃）。

### 5.13 `detect/face.rs` —— SCRFD 人脸检测

**输入输出约定**（Jonny001/Models-Pack-01 的 `scrfd_2.5g.onnx`，2026-08-30 实机验证）：
- 输入：`input [1,3,640,640]`，BGR 顺序，`(x*255-127.5)/128` 归一化到 [-1,1]。
- 输出：9 个张量，**输出名按序 `"0".."8"`**（非标准 `score_8` 命名）：
  - `0/1/2` = score（[N,1]），`3/4/5` = bbox（[N,4]），`6/7/8` = kps（[N,10]）。
  - 每尺度是**展平 `[N,C]` 布局**（N=H·W·2，行优先 `[h,w,anchor]`），不是 NCHW `[1,C,H,W]`。
  - bbox/kps 为 distance 回归值（单位 stride），score 已是 sigmoid 概率。

**后处理**（按实测展平布局解码）：
1. 3 尺度（stride 8/16/32）× 2 anchors 遍历；索引 `n = (hi*W+wi)*2+a`。
2. score 阈值 0.5 过滤。
3. distance-based bbox 解码：`bbox_data[n*4+k]*stride` → 相对 anchor 中心的 left/top/right/bottom。
4. 关键点解码：`kps_data[n*10+k*2..k*2+1]*stride` → 5 点 (dx, dy)。
5. 坐标按 `ratio = img_size/640` 映射回原图。
6. **NMS**（IoU 阈值 0.4）去重。

> v0.2.0 误用 NCHW `nchw_idx` + `score_8` 输出名会导致 `ort` 抛 `提取输出 score_8: Not Found`；v0.2.1 修复（提交 0a4c957），用真实人脸图（512×512 Lena）实测可正确解出 (221,203,369,406) 的人脸框。

**`DetectedFace`**：`{ bbox: [x1,y1,x2,y2], score, landmarks: [[f32;2];5] }`（原图像素坐标）。

### 5.14 `detect/landmark.rs` —— 2DFAN4 68 点关键点

**链路**（参考 FaceFusion `face_landmarker.py`）：
1. 由 bbox 计算相似仿射参数：`scale = 195 / bbox_size`（最长边缩放到 195px），bbox 中心对齐 crop 中心。
2. `warp_face` 裁剪到 256²（双线性采样）。
3. 预处理：BGR + 线性转 sRGB（[0,1]）。
4. 前向：输出 **`landmarks [1,68,3]`**（实测 2026-08-30 输出名，非标准 `landmarks_xyscore`；x/y 值域 0..64）。模型另含冗余输出 `heatmaps [1,68,64,64]`，本模块不消费。
5. 后处理：`x/64*256` 映射回 256 空间 → 逆仿射 `(x - t)/scale` 回原图坐标。

> v0.2.0 误用 `landmarks_xyscore` 输出名会导致 `ort` 抛 `提取输出 landmarks_xyscore: Not Found`；v0.2.1 修复（提交 0a4c957），用真实人脸图（512×512 Lena）实测 68 点关键点完整覆盖眉/眼/鼻/嘴/脸轮廓。

> 68 点顺序为 dlib/FAN 标准：0-16 轮廓、17-26 眉、27-35 鼻、36-47 眼、48-67 嘴。

### 5.15 `detect/segment.rs` —— BiSeNet 19 类分割

**链路**（参考 FaceFusion `face_parser`）：
1. bbox 裁剪（clamp 到图像边界）→ resize 512²。
2. 预处理：RGB + ImageNet 归一化 `(x - mean)/std`（mean=[0.485,0.456,0.406]，std=[0.229,0.224,0.225]）。
3. 前向：`output [1,19,512,512]` logits。
4. `argmax` → 512² 类别。
5. 最近邻 resize 回 crop 尺寸（保持类别）。
6. 放回原图坐标（其余区域 background=0）。

**`SegMask`**：`{ width, height, classes: Vec<u8> }`（行优先类别索引），
`to_mask(&[CLASS_SKIN])` 把指定类别转成 0/1 `ImageBuf` 掩码供追色算子使用。

类别常量：`CLASS_SKIN=1`、`CLASS_HAIR=17`、`CLASS_UPPER_LIP=12`、`CLASS_LOWER_LIP=13`。

### 5.16 `ai/mod.rs` —— RetouchEngine 门面

**职责**：持有 4 个模型的 `Option` 会话（惰性加载，缺失即降级）：

```rust
pub struct RetouchEngine {
    face_detector: Option<FaceDetector>,   // scrfd_2.5g.onnx
    landmarker: Option<Landmarker>,        // 2dfan4.onnx
    segmenter: Option<Segmenter>,          // bisenet_resnet_34.onnx
    enhancer: Option<Enhancer>,            // gpen_bfr_512.onnx
}
```

**降级策略**：模型文件不存在或加载失败 → `None` → 对应方法返回 `None` →
调度层跳过该 AI 步骤，管线退化为纯参数处理。加载失败打印 `eprintln!` 日志。

**关键方法**：
- `detect_faces(&mut self, img) -> Option<Vec<DetectedFace>>`
- `detect_landmarks(&mut self, img, bbox) -> Option<[[f32; 2]; 68]>`
- `segment_face(&mut self, img, bbox) -> Option<SegMask>`
- `enhance_face(&mut self, img, bbox, blend) -> Option<ImageBuf>`

**美型点生成**（纯算法，不依赖推理，可单测）：

- `auto_beauty_points(faces, w, h)`：SCRFD 5 点 → **大眼**。
  每眼上下左右 4 方向放置向外推的控制点；眼睛半径按两眼间距估算 `r = eye_dist/6`，
  推拉强度 `k = r * 0.12`。一张脸 → 2 眼 × 4 方向 = 8 个液化点。
- `face_beauty_points(landmarks, w, h, face_slim)`：68 点 → **瘦脸 + 大眼**。
  - 瘦脸：下颌轮廓点（3..=13）朝脸部中心（鼻尖 33）向内推，强度 `dist * 0.02 * slim`（slim = face_slim/50）。
  - 大眼：双眼轮廓点（36..=41 / 42..=47）从眼中心向外推，强度 `dist * 0.08`。
  - 11 个瘦脸点 + 12 个大眼点 = 23 个液化点。

### 5.17 `export.rs` —— 16bit TIFF / 8bit PNG 编码

**`encode_tiff(img)`**：手写最小 TIFF（16bit RGB、uncompressed、little-endian `II`）。

```
布局：header(8) + IFD count(2) + 10×entry(120) + next IFD(4) + BitsPerSample(6) + 像素数据
IFD 条目：ImageWidth(256) / ImageLength(257) / BitsPerSample(258,=16,16,16) /
         Compression(259,=1) / Photometric(262,=2 RGB) / StripOffsets(273) /
         SamplesPerPixel(277,=3) / RowsPerStrip(278) / StripByteCounts(279) /
         PlanarConfiguration(284,=1 chunky)
像素：f32 0..1 → u16 0..65535（保持 16bit 全链路精度，避免 8bit 色带）
```

**`encode_png(img)`**：线性 → sRGB → 8bit PNG（预览用，经 `image` crate）。

---

## 6. 调度层 crates/scheduler

**文件**：`crates/scheduler/src/lib.rs`（404 行）

**定位**：任务队列 + 事件广播 + 显存预算，是「UI 提交请求」与「引擎重算」之间的调度中枢。

### 6.1 `GpuBudget` —— 显存预算

```rust
pub const VRAM_8GB: usize = 8 * 1024 * 1024 * 1024; // RTX 3070 显存

pub struct GpuBudget {
    total: usize,               // 总量（8GB）
    used: Arc<AtomicUsize>,     // 已占用（原子计数）
}
// try_acquire(bytes) -> bool：CAS 循环尝试占用；release(bytes) 释放
```

**用途**：约束并发推理，避免多模型同时占满显存导致 OOM。

### 6.2 `Scheduler` 结构

```rust
pub struct Scheduler {
    tx_req: mpsc::Sender<EngineRequest>,        // 任务队列（容量 256）
    tx_evt: broadcast::Sender<EngineEvent>,     // 事件广播（容量 64）
    budget: GpuBudget,
}
```

**工作线程**（`tokio::spawn` 中）：

```rust
let engine = Arc::new(Mutex::new(RetouchEngine::new(&models_dir))); // AI 门面跨请求复用
while let Some(req) = rx_req.recv().await {
    run_one(&evt, &bgt, &engine, req).await;   // 串行执行
}
```

> **关键设计**：`RetouchEngine` 在调度线程内创建一次、跨请求复用（模型加载开销仅在首次）。
> 通过 `Arc<Mutex<>>` 保证单线程串行访问（ORT Session 非线程安全）。

### 6.3 `run_one` —— 单请求处理

```rust
async fn run_one(evt, budget, engine, req) {
    // 1. 显存预算：try_acquire(512*512*4*4 ≈ 4MB) 占位；失败 → Error::OutOfMemory
    // 2. 发 Progress(RawDecode, 0%)
    // 3. process_request(&req, engine)（同步执行，含解码/AI/管线/导出）
    // 4. budget.release(...)
    // 5. 成功 → Progress(Encode, 100%) + Done；失败 → Error(Decode, msg)
}
```

> 注：目前同步执行在 Tokio worker 线程内（M1 骨架）；后续可改 `spawn_blocking`
> 承接 CPU 密集任务，避免阻塞 async runtime。

### 6.4 `process_request` —— 重算主流程（核心）

```rust
fn process_request(req, engine) -> Result<String, String> {
    // 1. 读文件 + 解码（decode_auto 按扩展名分发）
    // 2. Preview/Base 模式：降采样（最长边默认 1600，min 320）
    // 3. Base 模式短路：写 {raw}.base.png（仅解码+降采样，不跑管线）
    // 4. recipe_to_pipeline(&req.recipe, w, h)
    // 5. AI 检测与增强（Mutex 锁引擎）：
    //    5a. detect_faces 全部人脸
    //    5b. 美型：68 点瘦脸/大眼（关键点模型缺失回退 5 点大眼）
    //    5c. 磨皮：GPEN enhance_face（blend = ka/100）
    //    5d. 追色：参考图皮肤色调 → build_region_transfer_lut（现场烘焙 LUT）
    // 6. process(&img, &pipeline) 16bit 全链路
    // 7. 导出：
    //    - Preview：只写 {raw}.out.png（8bit，快）
    //    - Export：写 {raw}.out.{tiff|png}（全分辨率）+ {raw}.out.png（预览）
    //    导出目录：export_dir 指定则写入该目录，否则与源文件同目录
}
```

**`recipe_to_pipeline` 映射**：

| Recipe 字段 | Pipeline 字段 | 映射逻辑 |
|---|---|---|
| `base.*` | `tone` | 直接映射（exposure/contrast/saturation/temperature/tint/curves/grain/vignette） |
| `beauty.manual_points` | `beauty_points` | 归一化控制点 → `LiquifyPoint { radius: 0.12 }` |
| `inpaint[]` | `inpaint_mask` | 每区域 `polygon_to_mask` → `merge_mask` 合并 |
| `filter` | `filter_lut` | `builtin_filter_lut(id)` → `blended(intensity)` |
| `neutral_gray` / `color` | 管线输入 | 由 AI 步骤在 `process_request` 中动态生成（蒙版/LUT） |

> **追色 LUT 现场烘焙**：`color.reference_path` 存在时，解码参考图 → 检测参考人脸 →
> 双方 BiSeNet 分割皮肤 mask → `build_region_transfer_lut` 烘焙 33³ LUT 注入管线。

---

## 7. Tauri 应用层 apps/desktop

**目录**：`apps/desktop/src-tauri/`

### 7.1 `main.rs` —— 入口与命令注册

```rust
fn main() {
    tauri::Builder::default()
        .plugin(tauri_plugin_dialog::init())   // 原生文件对话框
        .plugin(tauri_plugin_opener::init())   // 打开文件/目录
        .invoke_handler(tauri::generate_handler![
            commands::create_project,  commands::open_project,     commands::list_projects,
            commands::import_photos,   commands::list_photos,      commands::save_recipe,
            commands::get_recipe,      commands::submit_render,    commands::read_preview,
            commands::read_base,       commands::save_preset_cmd,  commands::list_presets_cmd,
            commands::get_settings,    commands::save_setting,     commands::reveal_in_explorer,
            models::list_model_packages_cmd, models::download_model_cmd,
        ])
        .setup(|app| { state::init(app.handle())?; Ok(()) })
        .run(tauri::generate_context!())
}
```

发布版隐藏控制台：`#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]`。

### 7.2 `state.rs` —— 应用状态

```rust
pub struct AppState {
    pub store: Store,          // SQLite 数据门面
    pub scheduler: Scheduler,  // 任务调度器
}
```

`init`：`app_data_dir/pixcake.db` 打开数据库 + `Scheduler::new(models)` +
`events::spawn_forwarder` 启动事件转发 + `app.manage(AppState)` 注入全局状态。

### 7.3 `events.rs` —— 事件转发

```rust
pub const EVENT_NAME: &str = "engine://event";
// 订阅 scheduler 广播，emit 到 WebView；Lagged 丢弃（积压），Closed 退出
```

### 7.4 `commands.rs` —— IPC 命令（18 个）

| 命令 | 签名 | 说明 |
|---|---|---|
| `create_project` | `(name) -> Project` | 新建项目，在 app_data/libraries/ 建目录 |
| `open_project` | `(dir) -> (Project, Vec<Photo>)` | **扫描目录**建（或复用同名）项目并导入去重 |
| `list_projects` | `-> Vec<Project>` | |
| `import_photos` | `(project_id, paths) -> Vec<Photo>` | 尺寸经 `probe_dimensions` 探测 |
| `list_photos` | `(project_id) -> Vec<Photo>` | |
| `save_recipe` | `(photo_id, recipe) -> ()` | recipe JSON 入库 |
| `get_recipe` | `(photo_id) -> Option<Recipe>` | |
| `submit_render` | `(photo_id, recipe, scope, export_dir?, preview_max_edge?, export_format?)` | scope 字符串 → `Scope` 枚举，入队 |
| `read_preview` | `(photo_id) -> base64` | **只传 ID**，后端查路径读 `{raw}.out.png`（防任意文件读） |
| `read_base` | `(photo_id) -> base64` | 同上，读 `{raw}.base.png`（WebGL 底图） |
| `save_preset_cmd` | `(name, recipe) -> Preset` | |
| `list_presets_cmd` | `-> Vec<Preset>` | |
| `get_settings` | `-> BTreeMap<String,String>` | 白名单键（theme/preview_max_edge/export_format/export_dir/import_dir/last_project） |
| `save_setting` | `(key, value)` | upsert |
| `reveal_in_explorer` | `(path)` | Windows `explorer /select,` |
| `list_model_packages_cmd` | `-> Vec<(ModelSpec, ModelStatus)>` | |
| `download_model_cmd` | `(id) -> path` | `spawn_blocking` 下载（ureq） |

**安全设计**（`read_preview` / `read_base`）：前端只传 `photo_id`，实际路径由后端从
数据库查询并拼接 —— **不暴露任意文件读取能力**（防路径穿越）。

### 7.5 `models.rs` —— 模型资源包管理

- `MODEL_PACKAGES`：静态清单（4 个开源 ONNX 模型，URL 用 hf-mirror.com 镜像）。
- `list_model_packages(app)`：清单 + 本地状态（已下载/未下载）。
- `download_model(app, id)`：`ureq` 同步下载到 `app_data/models/{id}.onnx`，由
  `spawn_blocking` 包裹（不阻塞 UI）。

---

## 8. 前端 React 层

**目录**：`apps/desktop/src/`

```
src/
  main.tsx                    入口
  App.tsx                     全局状态编排（598 行）
  api/
    client.ts                 IPC 封装（invoke/listen）
    types.ts                  与 bus 一一对应的前端类型（serde JSON 往返）
  components/
    canvas/Canvas.tsx         画布容器（WebGL 或 <img> 二选一）
    canvas/WebGLPreview.tsx   WebGL2 实时预览（437 行）
    panels/NeutralGrayPanel.tsx / BeautyPanel.tsx / ColorPanel.tsx /
           BasePanel.tsx / InpaintPanel.tsx / FilterPanel.tsx / PresetPanel.tsx /
           ModelManager.tsx
    SettingsModal.tsx         设置弹窗
```

### 8.1 `App.tsx` —— 状态编排（核心）

**状态**：

```ts
const [projects, setProjects] = useState<Project[]>([]);
const [projectId, setProjectId] = useState<string | null>(null);
const [photos, setPhotos] = useState<Photo[]>([]);
const [photo, setPhoto] = useState<Photo | null>(null);
const [recipe, setRecipe] = useState<Recipe>(defaultRecipe());
const [previewSrc, setPreviewSrc] = useState<string | null>(null); // 后端 PNG 预览
const [baseSrc, setBaseSrc] = useState<string | null>(null);       // WebGL 底图
const [progress, setProgress] = useState<number | null>(null);
// refs
const renderTimer = useRef<number | null>(null);      // 200ms 防抖
const baseCache = useRef(new Map<string, string>());  // photoId → 底图 base64
const pendingBase = useRef<string | null>(null);      // 在途底图请求
```

**WebGL 可用性判定**：

```ts
/** 是否纯参数编辑（无 AI 依赖操作）→ 可走 WebGL 实时预览。 */
function webglEligible(r: Recipe): boolean {
  return (
    !r.neutral_gray.enabled && !r.beauty.enabled &&
    r.beauty.manual_points.length === 0 &&
    r.inpaint.length === 0 && !r.color.enabled
  );
}
```

**编辑分流**（`updateRecipe`，核心决策逻辑）：

```ts
function updateRecipe(next: Recipe) {
  setRecipe(next);
  api.saveRecipe(photo.id, next);                    // 编辑即保存
  if (webglEligible(next)) {
    // WebGL 模式：清空后端渲染定时器，取底图 → GPU 即时渲染（滑块零延迟）
    renderTimer.current = null;
    ensureBase(photo);
    return;
  }
  // 后端模式：200ms 防抖提交 Preview 渲染
  renderTimer.current = window.setTimeout(() => {
    api.submitRender(photo.id, next, "preview", { previewMaxEdge });
  }, 200);
}
```

**底图缓存与防重入**（`ensureBase`）：

```ts
function ensureBase(p: Photo) {
  const cached = baseCache.current.get(p.id);
  if (cached) { setBaseSrc(cached); return; }   // 命中缓存
  if (pendingBase.current === p.id) return;     // 已在途，防重复提交
  pendingBase.current = p.id;
  api.submitRender(p.id, defaultRecipe(), "base", { previewMaxEdge });
}
```

**事件分流**（done 事件按 `pendingBase` 区分读取方式）：

```ts
if (pendingBase.current === e.photo_id) {
  // Base 完成 → 读底图
  pendingBase.current = null;
  api.readBase(e.photo_id).then((b64) => {
    baseCache.current.set(e.photo_id, b64);
    setBaseSrc(`data:image/png;base64,${b64}`);
  });
} else {
  // Preview 完成 → 读调色预览
  api.readPreview(e.photo_id).then((b64) => setPreviewSrc(`data:image/png;base64,${b64}`));
}
```

**批量导出**（`exportAll`）：对项目全部照片提交 `submitRender(scope:"export")`，
后台队列逐个处理；`batchTotal`/`batchDone` refs 累计进度。

**键盘快捷键**：Ctrl+O 打开项目 / Ctrl+I 导入 / Ctrl+E 导出 / Ctrl+Shift+E 批量导出 /
Ctrl+, 设置 / Ctrl+R 定位结果 / Esc 关闭弹窗。用 ref 保存最新闭包，effect 只装一次监听。

### 8.2 `Canvas.tsx` —— 画布容器

```ts
interface Props {
  previewSrc: string | null;          // 后端 PNG 预览
  webglSrc?: string | null;           // WebGL 底图
  webglRecipe?: Recipe | null;        // 实时调色参数
  // ...（drawing/draftPoints/onImageClick 祛瑕绘制支持）
}
// 渲染优先级：webglSrc && webglRecipe ? <WebGLPreview/> : previewSrc ? <img> : 占位
```

祛瑕绘制：点击回调归一化坐标 → `draftPoints`，SVG overlay 画多边形。

### 8.3 `WebGLPreview.tsx` —— WebGL2 实时预览（M6 亮点）

**原理**：底图（后端 `Scope::Base` 仅解码+降采样，未调色）上传为 GPU 纹理，
fragment shader **逐项复刻引擎数学**，滑块拖动即时重渲染。

**shader 与引擎对照表**：

| 引擎函数（tone.rs / lut.rs） | WebGL shader 实现 | 说明 |
|---|---|---|
| `tone_pixel` 白平衡 | `c.r *= 1+temp*0.5; c.b *= 1-temp*0.5; c.g *= 1+tint*0.3` | 完全一致 |
| 曝光 `2^EV` | `c *= exp2(uExposure)` | 完全一致 |
| 对比度 `(c-0.5)*cf+0.5` | `c = (c-0.5)*(1+uContrast/100)+0.5` | 完全一致 |
| 饱和度 Rec.709 | `gray = dot(c, vec3(0.2126,0.7152,0.0722))` | 完全一致 |
| `curve_lookup` 分段线性 | `curveLookup()` GLSL 版 | 一致（≤32 锚点） |
| `vignette_factor` | `v = 1 - (uVignette/100) * d*d` | 一致 |
| `hash_noise` 颗粒 | `hashNoise()` GLSL 版（uint 哈希） | 一致 |
| `builtin_filter_lut` 滤镜 | `uFilterMode`/`uFilterMults`/`uFilterAmt` | 模式1通道倍乘 / 模式2黑白 mix |

**技术要点**：
- 全屏三角形（3 顶点）+ `UNPACK_FLIP_Y_WEBGL`（vUv.y=1 对应图像顶部）。
- rAF 节流：参数变更只更新 ref 并 `requestRender()`，合并同帧多次更新。
- `ResizeObserver` + `devicePixelRatio`(≤2) 的 contain 适配。
- **降级**：WebGL2 不可用（`getContext` 返回 null）→ `setFailed(true)` → 渲染 `<img>` 静态预览。

### 8.4 `api/client.ts` —— IPC 封装

```ts
export const api = {
  submitRender: (photoId, recipe, scope: "base" | "preview" | "export", opts?) =>
    invoke("submit_render", { photoId, recipe, scope, exportDir: opts?.exportDir ?? null,
                              previewMaxEdge: opts?.previewMaxEdge ?? null,
                              exportFormat: opts?.exportFormat ?? null }),
  onEngineEvent: (handler) => listen<EngineEvent>("engine://event", (ev) => handler(ev.payload)),
  // ... 其余命令一一封装
};
```

---

## 9. 参数传递链路全解析

### 9.1 Recipe 生命周期（完整闭环）

```
┌─ 编辑阶段 ─────────────────────────────────────────────────────────────┐
│ 面板滑块 → updateRecipe(next) → api.saveRecipe(photoId, next)          │
│   → invoke("save_recipe", { photoId, recipe })                          │
│   → Store::save_recipe → serde_json → INSERT INTO recipe(data) upsert   │
└─────────────────────────────────────────────────────────────────────────┘
┌─ 渲染阶段 ─────────────────────────────────────────────────────────────┐
│ api.submitRender(photoId, recipe, scope, opts)                          │
│   → invoke("submit_render", ...)                                        │
│   → commands::submit_render：查 get_photo 得 raw_path，组装 EngineRequest│
│   → Scheduler::enqueue → Tokio mpsc → run_one → process_request         │
│   → decode_auto → recipe_to_pipeline → AI → process(16bit 管线) → 导出   │
│   → EngineEvent::Done → events.rs → WebView "engine://event"            │
│   → App.tsx 分流 → read_preview/read_base → base64 → 显示                │
└─────────────────────────────────────────────────────────────────────────┘
┌─ 重开阶段 ─────────────────────────────────────────────────────────────┐
│ selectPhoto(p) → api.getRecipe(p.id) → Store::get_recipe → 反序列化 Recipe│
│ → webglEligible(recipe) ? ensureBase(p) : （等用户编辑触发预览）          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 9.2 三种渲染 scope 的差异

| | `base` | `preview` | `export` |
|---|---|---|---|
| 降采样 | ✅ 最长边 1600（可配） | ✅ 最长边 1600（可配） | ❌ 全分辨率 |
| 跑管线 | ❌ 短路写 `{raw}.base.png` | ✅ | ✅ |
| AI 推理 | ❌ | ✅ | ✅ |
| 输出 | `{raw}.base.png`（8bit PNG） | `{raw}.out.png`（8bit PNG） | `{raw}.out.{tiff\|png}` + `{raw}.out.png` |
| 用途 | WebGL 实时预览底图 | AI 操作的后端 PNG 预览 | 全分辨率成品 |

### 9.3 AI 功能链路

**磨皮**（`neutral_gray.enabled && ka>0`）：
```
人脸检测(SCRFD) → 每张脸 enhance_face(GPEN, blend=ka/100) → 替换 img
```

**美型**（`beauty.enabled` 或手动点）：
```
detect_faces → 每张脸 detect_landmarks(2DFAN4, 68 点)
  → face_beauty_points（瘦脸 3..=13 + 大眼 36..47，共 23 点）
  → 若 68 点不可用，回退 auto_beauty_points（SCRFD 5 点 → 大眼 8 点）
  → pipeline.beauty_points → liquify 反向采样
```

**追色**（`color.enabled && reference_path`）：
```
解码参考图 → detect_faces(参考图) → 取第一张脸 bbox
→ segment_face(目标图, bbox) + segment_face(参考图, bbox) → 皮肤 mask
→ build_region_transfer_lut（Lab μ/σ 迁移，33³ LUT）
→ pipeline.color_lut → 三线性插值应用
```

**祛瑕**（`inpaint[]` 非空）：
```
每个 region：polygon_to_mask（射线法）→ merge_mask 合并
→ pipeline.inpaint_mask → inpaint 迭代扩散（4 次）
```

---

## 10. SQLite 数据表结构

```sql
-- 项目（客片组）
CREATE TABLE project(
  id TEXT PRIMARY KEY, name TEXT, created_at INTEGER,
  root_path TEXT, thumb TEXT);

-- 照片
CREATE TABLE photo(
  id TEXT PRIMARY KEY, project_id TEXT,
  raw_path TEXT, proxy_path TEXT, result_path TEXT,
  width INTEGER, height INTEGER, status TEXT,      -- pending/retouched/exported
  created_at INTEGER);

-- 编辑参数（photo_id 主键，data 为 Recipe JSON）
CREATE TABLE recipe(
  photo_id TEXT PRIMARY KEY,
  data TEXT,
  updated_at INTEGER);

-- 预设
CREATE TABLE preset(
  id TEXT PRIMARY KEY, name TEXT, scope TEXT,     -- 推荐 / 我的样片
  recipe_json TEXT, lut_path TEXT);

-- 模型元数据（模型文件不入库）
CREATE TABLE model(
  id TEXT PRIMARY KEY, name TEXT, path TEXT,
  version TEXT, hash TEXT, enabled INTEGER);

-- 用户设置（key/value）
CREATE TABLE setting(
  key TEXT PRIMARY KEY, value TEXT);
```

**关系**：`project 1─N photo`；`photo 1─1 recipe`（主键关联）；
`photo ─N─ 大文件`（路径引用，不入库）。

---

## 11. 目录结构与代码规模

### 11.1 仓库结构

```
pixel-cake/
  crates/
    bus/         编辑协议（Recipe/Scope/EngineRequest/EngineEvent/ModelSpec）
    engine/      核心引擎（16bit 图像管线 + AI 推理 + 编解码 + 导出）
    scheduler/   调度层（Tokio 队列 + GpuBudget + 事件广播）
    storage/     SQLite 数据门面
  apps/
    desktop/
      src-tauri/ Tauri 后端（main/commands/state/events/models）
      src/       React 前端（App/Canvas/WebGLPreview/面板/api）
  scripts/       gen_icon.mjs（CI 内生成图标）
  docs/          操作手册.html
  README.md
```

### 11.2 代码规模（wc -l 统计，约 7297 行源码）

| 文件 | 行数 | 职责 |
|---|---|---|
| `apps/desktop/src/App.tsx` | 598 | 前端状态编排 |
| `apps/desktop/src/components/canvas/WebGLPreview.tsx` | 437 | WebGL2 实时预览 |
| `crates/scheduler/src/lib.rs` | 404 | 调度 + 重算主流程 |
| `crates/storage/src/lib.rs` | 383 | SQLite 门面 |
| `crates/engine/src/ai/mod.rs` | 306 | AI 门面 + 美型点生成 |
| `crates/bus/src/lib.rs` | 303 | 编辑协议 |
| `crates/engine/src/color/lut.rs` | 283 | 3D LUT |
| `crates/engine/src/base/tone.rs` | 269 | 基础调色 |
| `crates/engine/src/retouch/color_transfer.rs` | 273 | Lab 追色 |
| `crates/engine/src/image.rs` | 251 | 图像缓冲/算子抽象 |
| `crates/engine/src/detect/face.rs` | 234 | SCRFD 检测 |
| `crates/engine/src/infer.rs` | 168 | ORT 推理封装 |
| `crates/engine/src/retouch/enhance.rs` | 165 | GPEN 增强 |
| `crates/engine/src/retouch/inpaint.rs` | 158 | 祛瑕 |
| `crates/engine/src/pipeline.rs` | 124 | 16bit 全链路 |
| `crates/engine/src/retouch/beauty.rs` | 114 | 液化 |
| `crates/engine/src/raw.rs` | 320 | RAW/图像解码 |
| `crates/engine/src/export.rs` | 160 | TIFF/PNG 编码 |
| `crates/engine/src/detect/landmark.rs` | 157 | 2DFAN4 |
| `crates/engine/src/detect/segment.rs` | 228 | BiSeNet |
| `apps/desktop/src-tauri/src/commands.rs` | 289 | IPC 命令 |
| `apps/desktop/src/api/client.ts` | 81 | IPC 封装 |
| `apps/desktop/src/components/canvas/Canvas.tsx` | 116 | 画布容器 |

---

## 12. 关键设计决策与约定

### 12.1 为什么 16bit f32 全链路？

8bit 每通道 256 级在多次调整（白平衡×对比度×饱和度…）后必然出现色带与累积误差。
本项目的 `ImageBuf` 全程以 `f32` 平面存储，仅在编解码边界做 sRGB↔线性转换；
导出 16bit TIFF 保真（`encode_tiff` 手写编码避免依赖外部库）。

### 12.2 为什么追色/滤镜烘焙为 3D LUT？

色彩迁移（Lab 转换 + 统计）逐像素执行非常昂贵。离散化为 33³ 查找表后：
1. **预览**：只需三线性插值，性能提升数个量级。
2. **批量**：同一 LUT 可复用于整批照片（统一风格）。
3. **强度调节**：`blended(intensity)` 向恒等混合即可，无需重算迁移。

### 12.3 WebGL 实时预览 vs 后端全链路

| 维度 | WebGL（base 模式） | 后端（preview 模式） |
|---|---|---|
| 延迟 | 滑块零延迟（GPU 即时） | 200ms 防抖 + 后端渲染 |
| 覆盖 | 基础调色 + 滤镜 | 全部功能（含 AI） |
| 一致性 | shader 复刻引擎数学 | 引擎原生 |
| 切换 | `webglEligible(recipe)` 自动判定 | 含 AI 操作时自动回退 |

### 12.4 安全加固清单

- `ImageBuf::new`：`checked_mul` + `MAX_PIXELS`（2 亿）防恶意头 OOM。
- `decode_ppm`：头部宽高校验（返回 `Err` 而非 panic）。
- `read_preview` / `read_base`：只传 photo_id，后端查库拼路径，防任意文件读取。
- `from_srgb_rgba8`：`assert!(bytes.len() >= n*4)` 防越界。
- 模型文件不入库、不执行：只读字节喂 ORT。

### 12.5 降级策略（graceful degradation）

- 模型缺失/加载失败 → `RetouchEngine` 对应字段 `None` → AI 功能跳过，管线纯参数处理。
- CUDA EP 注册失败 → ort 静默回退 CPU。
- WebGL2 不可用 → `WebGLPreview` 降级 `<img>`。
- 关键点模型缺失 → 美型回退 5 点大眼。

### 12.6 并发模型

- `Store`：`Mutex<Connection>`（rusqlite 非 Sync，满足 Tauri State 约束）。
- `RetouchEngine`：`Arc<Mutex<>>` 在调度线程内串行访问（ORT Session 非线程安全）。
- `GpuBudget`：`Arc<AtomicUsize>` 原子计数，CAS 无锁占用。
- 事件：`broadcast` 通道（64 容量），Lagged 丢弃积压。

### 12.7 测试策略

- 引擎层：每个算子模块内嵌 `#[cfg(test)]` 单测（恒等性、边界、形状、数值）。
- 存储层：`Store::in_memory()` 内存库测试（roundtrip）。
- 后端编译/测试/打包：GitHub Actions（本地缺 MSVC 链接器，只能 `rustc --test --emit=metadata` 类型检查）。

---

## 13. 已知风险与后续工作

### 13.1 主要风险

| 风险 | 说明 | 缓解 |
|---|---|---|
| ~~**AI 模型输入输出约定未实机验证**~~ | 已 2026-08-30 验证（4 个 ONNX，~467MB 全部下载到本地用 onnxruntime 1.29.0 + 真实人脸图端到端实测） | ✅ SCRFD 展平布局修复、2DFAN4 输出名修复（提交 0a4c957 → v0.2.1）。BiSeNet/GPEN 之前已匹配 |
| CUDA 环境依赖 | ort 要求 CUDA ≥ 13.2 + cuDNN ≥ 9.23 | 随安装包内置运行时；注册失败回退 CPU |
| `body_slim`/`neck_slim`/`face_full` 未接引擎 | UI 有参数，液化仅消费手动点 + AI 瘦脸/大眼 | 后续在 `face_beauty_points` 扩展 |

### 13.2 剩余工作（M6）

- [x] 模型实机验证（4 个 ONNX 模型的输入/输出约定）— 2026-08-30 完成
- [ ] 预设市场（在线下载预设 LUT）
- [ ] 性能调优（`spawn_blocking` 承接 CPU 密集、OpCost 驱动显存调度、WebGL 预览替代 PNG 轮询）
- [ ] `.cube` 滤镜库接入（`FilterPanel` 目前用内置 4 滤镜）

---

*本文档由源码注释整理，与 `crates/*/src/**/*.rs` 及 `apps/desktop/src/**/*.tsx` 同步维护。*
